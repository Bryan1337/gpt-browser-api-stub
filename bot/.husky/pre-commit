#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔎 Running type checks..."
npm run type-check || exit 1

echo "🧹 Linting & formatting files..."
npx lint-staged || exit 1

echo "✅ Pre-commit passed"